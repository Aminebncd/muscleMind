{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}

<div class="dark:bg-secondary/60 light:bg-secondary_light p-8 rounded-lg w-5/6 mt-10 max-w-screen-lg backdrop-blur-md">
    <div class="my-1 px-8 py-6 mb-4 rounded-lg dark:dark:bg-primary/60 light:bg-primary_light/60">
        <h1 class="text-center font-bold text-2xl px-2 py-4 dark:text-white light:text-black">Update your email</h1>
    </div>

     <p class="text-lg font-medium mb-6 dark:text-white light:text-black">current email: {{user.email}}</p>

    {{ form_start(updateEmailForm) }}

    
    {{ form_end(updateEmailForm) }}
</div>
{% endblock %}
