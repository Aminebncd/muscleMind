{% extends 'base.html.twig' %}

{% block title %}Hello ExerciceController!{% endblock %}

{% block body %}

    <h1>ExerciceController index</h1>

    <ul>
        {% for exercice in exercices %}
            <li>{{ exercice}}</li>
        {% endfor %}
    </ul>

    <a href="{{ path('app_exercice_new') }}">Create new</a>
{% endblock %}
