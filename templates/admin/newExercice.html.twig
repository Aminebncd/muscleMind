{% extends 'base.html.twig' %}

{% block title %}Hello ExerciceController!{% endblock %}

{% block body %}
<div class="dark:bg-secondary/60 light:bg-secondary_light/60 dark:text-white light:text-black p-8 rounded-lg w-5/6 mt-10 max-w-screen-lg  backdrop-blur-md">
    <div class="mt-4 px-8 py-6 mb-8 rounded-lg dark:dark:bg-primary/60 light:bg-primary_light/60">
        <h1 class="text-center font-bold text-2xl px-2 py-4">Add or edit an exercice</h1>
    </div>
    {{form_start(exerciceForm)}}
    {{form_end(exerciceForm)}}
</div>

    <a href="{{ path('app_exercice') }}" class="mt-4 mx-auto dark:text-white light:text-black  font-medium hover:underline duration-150"><i class="fa-solid fa-arrow-left"></i> Back to index</a>
{% endblock %}


