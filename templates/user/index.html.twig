{% extends 'base.html.twig' %}

{% block title %}Hello UserController!{% endblock %}

{% block body %}
<a href="{{path ('app_user_updateUsername', {'id': user.id}) }}">update username</a> <br>
<a href="{{path ('app_register_updateEmail', {'id': user.id}) }}">update email</a> <br>
<a href="{{path ('app_register_updatePassword', {'id': user.id}) }}">update password</a> <br><br><br>

    
        {{user}} <br>
        {{user.email}} <br>
        {{user.score}} <br> <br>

        {% if latestTracking %}
        <p>See some change? keep track  <a href="{{ path ('app_user_newTrack')}}">here</a> !</p>
        <div>
            <h2>
                {{latestTracking.age}} years old <br>
                {# {{latestTracking.sex}} <br> #}
                {{latestTracking.height}}cm <br>
                {{latestTracking.weight}}kg 
                <a href="{{ path ('app_user_editTrack', {'id': latestTracking.id}) }}">edit</a>
                <a href="{{ path ('app_user_deleteTrack', {'id': latestTracking.id}) }}">delete</a>
            </h2>
        
        </div>
        {% else %}
        <p>Wanna track your progress? add your trackings <a href="{{ path ('app_user_newTrack')}}"> here</a> !</p>
        {% endif %}

        
        
        {% if user.performances %}
            Made progress? write everything down <a href="{{ path ('app_user_newPerf')}}"> here</a> ! <br><br>
            {% for performance in user.performances %}
                {{performance}} <a href="{{ path ('app_user_editPerf', {'id': performance.id}) }}">edit</a> <a href="{{ path ('app_user_deletePerf', {'id': performance.id}) }}">delete</a> <br>
            {% endfor %}
        {% else %}
            Wanna track your progress? add your performances <a href="#"> here</a> !
        {% endif %} <br><br>

        {% if user.programs %} 
            {% for program in user.programs %}
                <a href="{{ path('app_training_edit', {'id': program.id}) }}">{{ program }}</a> <br>
            {% endfor %}
        {% endif %} <br><br>

        {# {% if user.sessions %}
            {% for session in user.sessions %}
                {{session}} <br>
            {% endfor %}
        {% endif %} #}

        
        




{% endblock %}
