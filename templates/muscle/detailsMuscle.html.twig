{% extends 'base.html.twig' %}

{% block title %}Hello MuscleController!{% endblock %}

{% block body %}

    <h1>Details Muscle 
        {% if is_granted('ROLE_ADMIN') %}
            <a href="{{path ('app_muscle_edit', {'id' : muscle.id})}}"><i class="fa-solid fa-pen-to-square"></i></a>
            <a href="{{path ('app_muscle_delete', {'id' : muscle.id})}}"><i class="fa-solid fa-trash"></i></a>
        {% endif %}
    </h1>

    <p>Name: {{ muscle}}</p>
    <p>Function: {{muscle.muscleFunction}}</p>
   

    <a href="{{ path('app_muscleGroup_details', {'id' : muscleGroup.id}) }}">Back to muscle group</a>

   

{% endblock %}
